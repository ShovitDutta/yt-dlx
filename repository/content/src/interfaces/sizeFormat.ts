// Suggestion: Add JSDoc comments to the interface to explain what the function does and what kind of input it expects. Also, add JSDoc to the parameter and return type.
export default interface sizeFormat {
    (filesize: number): string | number;
}
